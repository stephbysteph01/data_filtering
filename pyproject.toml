[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "czrm-analysis"
version = "0.1.0"
description = "Analysis of CzRM reports for Roma Capitale"
authors = [ { name = "Your Name" } ]
dependencies = [
    "pandas",
    "numpy",
    "matplotlib",
    "openpyxl",
]

[tool.pytest.ini_options]
minversion = "6.0"
